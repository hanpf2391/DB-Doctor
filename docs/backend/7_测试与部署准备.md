# DB-Doctor 测试与部署准备

## 一、开发目标

完善项目测试、编写文档、准备发布，确保项目可以投入生产使用。

## 二、功能描述

编写单元测试、集成测试、README 文档、部署文档，并进行完整的功能验证。

## 三、详细开发任务

### 3.1 测试用例编写

#### 3.1.1 单元测试

**位置**: `src/test/java/com/dbdoctor/`

##### 1. 日志解析测试 (LogParserTest)

```java
@SpringBootTest
class LogParserTest {

    @Autowired
    private LogMonitorService logMonitorService;

    @Test
    void testParseSingleLog() {
        String rawLog = """
            # Time: 2024-01-22T10:30:00.123456Z
            # User@Host: root[root] @ localhost []
            # Query_time: 3.500000  Lock_time: 0.000100 Rows_sent: 5  Rows_examined: 500000
            SET timestamp=1705900200;
            SELECT * FROM orders WHERE user_id = 123;
            """;

        SlowLogEntry entry = logMonitorService.parseLog(rawLog);

        assertNotNull(entry);
        assertEquals(3.5, entry.getQueryTime());
        assertEquals(500000L, entry.getRowsExamined());
        assertTrue(entry.getSql().contains("SELECT * FROM orders"));
    }

    @Test
    void testParseMultiLineSql() {
        String rawLog = """
            # Time: 2024-01-22T10:30:05.654321Z
            # Query_time: 1.200000  Lock_time: 0.000050 Rows_sent: 100  Rows_examined: 10000
            SET timestamp=1705900205;
            SELECT
                u.id, u.name, o.order_id
            FROM users u
            LEFT JOIN orders o ON u.id = o.user_id
            WHERE u.status = 1;
            """;

        SlowLogEntry entry = logMonitorService.parseLog(rawLog);

        assertNotNull(entry);
        assertTrue(entry.getSql().contains("SELECT"));
        assertTrue(entry.getSql().contains("LEFT JOIN"));
    }
}
```

##### 2. SQL 诊断工具测试 (SqlDiagnosticsToolsTest)

```java
@SpringBootTest
class SqlDiagnosticsToolsTest {

    @Autowired
    private SqlDiagnosticsTools tools;

    @Test
    void testGetExecutionPlan() {
        String sql = "SELECT * FROM users WHERE id = 1";
        String plan = tools.getExecutionPlan(sql);

        assertNotNull(plan);
        assertTrue(plan.contains("query_cost"));
    }

    @Test
    void testGetTableSchema() {
        String schema = tools.getTableSchema("users");

        assertNotNull(schema);
        assertTrue(schema.contains("CREATE TABLE"));
    }

    @Test
    void testCompareSqlPerformance() {
        String oldSql = "SELECT * FROM orders WHERE user_id = 123";
        String newSql = "SELECT * FROM orders WHERE user_id = '123'";

        String result = tools.compareSqlPerformance(oldSql, newSql);

        assertNotNull(result);
        assertTrue(result.contains("cost"));
    }
}
```

##### 3. AI Agent 测试 (DBAgentTest)

```java
@SpringBootTest
class DBAgentTest {

    @Autowired
    private DBAgent dbAgent;

    @Test
    void testAnalyzeSlowLog() {
        String report = dbAgent.analyzeSlowLog(
            "test_db",
            "2024-01-22 10:30:00",
            5.2,
            0.001,
            1000000L,
            10L,
            "SELECT * FROM orders WHERE user_id = 123"
        );

        assertNotNull(report);
        assertTrue(report.contains("问题诊断") || report.contains("优化建议"));
    }
}
```

#### 3.1.2 集成测试

**位置**: `src/test/java/com/dbdoctor/integration/`

##### 端到端测试 (EndToEndTest)

```java
@SpringBootTest
@TestPropertySource(locations = "classpath:application-test.yml")
class EndToEndTest {

    @Autowired
    private LogMonitorService logMonitorService;

    @Autowired
    private AnalysisService analysisService;

    @Autowired
    private NotifyService notifyService;

    @Test
    void testFullPipeline() throws InterruptedException {
        // 1. 模拟写入慢查询日志
        String testLogPath = "/tmp/test-slow.log";
        writeTestLog(testLogPath);

        // 2. 等待监听服务解析
        Thread.sleep(5000);

        // 3. 验证分析结果
        // ...

        // 4. 验证通知发送
        // ...
    }
}
```

### 3.2 测试数据准备

#### 3.2.1 测试用慢查询日志

**位置**: `src/test/resources/test-slow.log`

```sql
# Time: 2024-01-22T10:30:00.123456Z
# User@Host: app[app] @ localhost []
# Query_time: 5.200000  Lock_time: 0.000100 Rows_sent: 10  Rows_examined: 1000000
SET timestamp=1705900200;
SELECT * FROM orders WHERE user_id = 123;

# Time: 2024-01-22T10:30:05.654321Z
# User@Host: root[root] @ localhost []
# Query_time: 3.100000  Lock_time: 2.500000 Rows_sent: 5  Rows_examined: 500000
SET timestamp=1705900205;
SELECT * FROM products WHERE category_id = 10 FOR UPDATE;

# Time: 2024-01-22T10:30:10.789012Z
# User@Host: app[app] @ localhost []
# Query_time: 2.500000  Lock_time: 0.000050 Rows_sent: 100  Rows_examined: 10000
SET timestamp=1705900210;
SELECT
    u.id, u.name, o.order_id
FROM users u
LEFT JOIN orders o ON u.id = o.user_id
WHERE u.status = 1;
```

#### 3.2.2 测试配置文件

**位置**: `src/test/resources/application-test.yml`

```yaml
db-doctor:
  log:
    slow-log-path: src/test/resources/test-slow.log

  notify:
    enabled-notifiers: none  # 测试时不发送通知

langchain4j:
  open-ai:
    api-key: test-key
    model-name: gpt-4
```

### 3.3 文档编写

#### 3.3.1 README.md

**位置**: `README.md`

```markdown
# DB-Doctor

> MySQL 慢查询智能诊疗系统

DB-Doctor 是一款非侵入式的 MySQL 慢查询智能诊断中间件，通过实时监听慢查询日志，利用 AI Agent 分析根因并推送优化建议。

## 特性

- 🔍 **实时监听**: 基于 Apache Commons IO 的 Tailer 实现，类似 Linux tail -f
- 🤖 **AI 智能诊断**: 基于 LangChain4j 的 ReAct 模式，自主调用诊断工具
- 📊 **精准分析**: 对比慢查询日志数据和数据库实际执行计划
- 📢 **多渠道通知**: 支持邮件、钉钉、飞书、企业微信
- 🛡️ **安全可靠**: 只读数据库连接，严禁危险操作

## 快速开始

### 环境要求

- JDK 17+
- MySQL 5.7+ / 8.0+
- OpenAI API Key 或兼容接口

### 安装部署

1. **下载解压**
   ```bash
   tar -xzf db-doctor-1.0.0-bin.tar.gz
   cd db-doctor-1.0.0
   ```

2. **修改配置**
   ```bash
   vi config/application.yml
   ```

   必填配置：
   - MySQL 连接信息
   - 慢查询日志路径
   - OpenAI API Key

3. **启动应用**
   ```bash
   ./bin/startup.sh
   ```

4. **查看日志**
   ```bash
   tail -f logs/db-doctor.log
   ```

## 配置说明

### 数据源配置

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/information_schema
    username: db_doctor
    password: your_password
```

**重要**: 使用只读账户，权限：SELECT, SHOW VIEW

### 慢查询日志配置

```yaml
db-doctor:
  log:
    slow-log-path: /var/log/mysql/slow-query.log
```

### AI 配置

```yaml
langchain4j:
  open-ai:
    api-key: sk-xxx
    model-name: gpt-4
    base-url: https://api.openai.com/v1  # 支持兼容接口
```

### 通知配置

```yaml
db-doctor:
  notify:
    enabled-notifiers: email,webhook
    email:
      enabled: true
      to:
        - dba@example.com
```

## 工作原理

```
MySQL slow.log
    ↓
LogMonitorService (实时监听 + 解析)
    ↓
AnalysisService (异步分析)
    ↓
DBAgent (AI Agent)
    ↓
调用工具:
  - getExecutionPlan()
  - getTableSchema()
  - getTableStatistics()
  - getLockInfo()
  - getIndexSelectivity()
    ↓
生成诊断报告
    ↓
NotifyService (发送通知)
```

## 诊断示例

**慢查询日志**:
```
Query_time: 5.2s
Rows_examined: 1000000
SQL: SELECT * FROM orders WHERE user_id = 123
```

**AI 诊断报告**:
```markdown
## 问题诊断
- 根本原因：user_id 字段类型不匹配导致索引失效
- 严重程度：高

## 优化建议
```sql
-- 错误写法
SELECT * FROM orders WHERE user_id = 123

-- 正确写法
SELECT * FROM orders WHERE user_id = '123'
```

## 预期效果
- 查询成本从 1000.5 降低到 10.25
- 性能提升约 99.0%
```

## 常见问题

### 1. 数据库权限问题

**错误**: `Access denied for user`

**解决**: 确保数据库账户有以下权限：
```sql
GRANT SELECT ON information_schema.* TO 'db_doctor'@'%';
GRANT SHOW VIEW ON *.* TO 'db_doctor'@'%';
FLUSH PRIVILEGES;
```

### 2. 慢查询日志路径错误

**错误**: `File not found: /var/log/mysql/slow.log`

**解决**: 检查 MySQL 配置：
```bash
mysqladmin variables | grep slow_query_log
```

### 3. AI 调用失败

**错误**: `OpenAI API request failed`

**解决**:
- 检查 API Key 是否正确
- 检查网络连接
- 尝试使用兼容接口（如 Azure OpenAI）

## 项目结构

```
db-doctor/
├── bin/                    # 启动脚本
├── config/                 # 配置文件
├── lib/                    # 依赖 JAR
└── logs/                   # 日志文件
```

## 技术栈

- Java 17
- Spring Boot 3.x
- LangChain4j
- Apache Commons IO
- Druid
- MySQL

## 贡献指南

欢迎提交 Issue 和 Pull Request！

## 许可证

[Apache License 2.0](LICENSE)
```

#### 3.3.2 部署文档 (DEPLOYMENT.md)

**位置**: `DEPLOYMENT.md`

```markdown
# DB-Doctor 部署文档

## 前置条件

### 1. Java 环境

检查 Java 版本：
```bash
java -version  # 需要 17+
```

### 2. MySQL 配置

确保 MySQL 开启慢查询日志：
```sql
SET GLOBAL slow_query_log = 'ON';
SET GLOBAL long_query_time = 2;
SET GLOBAL log_queries_not_using_indexes = 'ON';
```

查看慢查询日志路径：
```sql
SHOW VARIABLES LIKE 'slow_query_log_file';
```

### 3. 数据库账户

创建只读账户：
```sql
CREATE USER 'db_doctor'@'%' IDENTIFIED BY 'your_password';
GRANT SELECT ON information_schema.* TO 'db_doctor'@'%';
GRANT SHOW VIEW ON *.* TO 'db_doctor'@'%';
FLUSH PRIVILEGES;
```

## 部署步骤

### 方式 1：直接部署

1. **下载安装包**
   ```bash
   wget https://github.com/xxx/db-doctor/releases/download/v1.0.0/db-doctor-1.0.0-bin.tar.gz
   ```

2. **解压**
   ```bash
   tar -xzf db-doctor-1.0.0-bin.tar.gz
   cd db-doctor-1.0.0
   ```

3. **修改配置**
   ```bash
   vi config/application.yml
   ```

4. **启动**
   ```bash
   chmod +x bin/*.sh
   ./bin/startup.sh
   ```

5. **验证**
   ```bash
   tail -f logs/db-doctor.log
   ```

### 方式 2：Docker 部署

```dockerfile
FROM openjdk:17-jdk-slim

WORKDIR /app

COPY .. /app/

EXPOSE 8080

CMD ["java", "-jar", "lib/db-doctor-1.0.0.jar"]
```

### 方式 3：Systemd 服务

创建服务文件 `/etc/systemd/system/db-doctor.service`:

```ini
[Unit]
Description=DB-Doctor MySQL Slow Query Monitor
After=network.target mysql.service

[Service]
Type=simple
User=dbdoctor
Group=dbdoctor
WorkingDirectory=/opt/db-doctor
ExecStart=/opt/db-doctor/bin/startup.sh
ExecStop=/opt/db-doctor/bin/shutdown.sh
Restart=on-failure
RestartSec=10

[Install]
WantedBy=multi-user.target
```

启动服务：
```bash
sudo systemctl daemon-reload
sudo systemctl start db-doctor
sudo systemctl enable db-doctor
sudo systemctl status db-doctor
```

## 配置调优

### JVM 参数

根据服务器内存调整：

```bash
# 小型实例（2GB 内存）
JVM_OPTS="-Xms256m -Xmx512m"

# 中型实例（4GB 内存）
JVM_OPTS="-Xms512m -Xmx1024m"

# 大型实例（8GB+ 内存）
JVM_OPTS="-Xms1024m -Xmx2048m"
```

### 线程池配置

```yaml
db-doctor:
  thread-pool:
    log-parser:
      core-size: 4      # 根据日志量调整
      max-size: 8
    ai-analysis:
      core-size: 2      # 根据 AI API 限流调整
      max-size: 4
```

## 监控指标

### 应用监控

- JVM 堆内存使用率
- 线程池队列长度
- AI 调用成功率
- 日志解析速率

### 业务监控

- 慢查询捕获数量
- 通知发送成功率
- 平均诊断耗时

## 故障排查

### 1. 应用无法启动

检查日志：
```bash
tail -f logs/db-doctor.log
```

常见原因：
- Java 版本不匹配
- 配置文件格式错误
- 数据库连接失败

### 2. 日志监听失败

检查文件权限：
```bash
ls -l /var/log/mysql/slow.log
```

确保 `db-doctor` 运行用户有读权限：
```bash
chmod +r /var/log/mysql/slow.log
```

### 3. AI 诊断超时

调整超时时间：
```yaml
langchain4j:
  open-ai:
    timeout: 120s  # 增加到 120 秒
```

## 备份与恢复

### 配置备份

```bash
cp config/application.yml config/application.yml.bak
```

### 日志备份

```bash
tar -czf logs-backup-$(date +%Y%m%d).tar.gz logs/
```

## 升级指南

1. **停止应用**
   ```bash
   ./bin/shutdown.sh
   ```

2. **备份配置**
   ```bash
   cp config/application.yml /tmp/application.yml.bak
   ```

3. **替换文件**
   ```bash
   rm -rf lib/db-doctor-*.jar
   cp /path/to/new/lib/db-doctor-*.jar lib/
   ```

4. **恢复配置**
   ```bash
   cp /tmp/application.yml.bak config/application.yml
   ```

5. **启动应用**
   ```bash
   ./bin/startup.sh
   ```
```

### 3.4 功能验证清单

- [ ] 日志监听功能正常
- [ ] 日志解析准确
- [ ] 数据库连接正常
- [ ] 诊断工具全部可用
- [ ] AI Agent 能够分析
- [ ] 邮件通知发送成功
- [ ] Webhook 通知发送成功
- [ ] 配置文件外部化
- [ ] 启动脚本可用
- [ ] 日志输出正常
- [ ] 异常处理完善
- [ ] 文档完整

## 四、验收标准

1. ✓ 所有单元测试通过
2. ✓ 集成测试通过
3. ✓ README 文档完整
4. ✓ 部署文档清晰
5. ✓ 功能验证清单全部完成
6. ✓ 能够在测试环境成功部署
7. ✓ 能够在生产环境稳定运行

## 五、发布清单

- [ ] 代码审查完成
- [ ] 所有测试通过
- [ ] 文档更新完成
- [ ] 版本号更新
- [ ] Git Tag 打包
- [ ] Release Note 编写
- [ ] 安装包构建
- [ ] 发布到 GitHub/私服

## 六、注意事项

1. **测试环境**: 必须在测试环境完整验证
2. **配置检查**: 发布前检查配置文件
3. **数据备份**: 生产环境部署前备份数据
4. **回滚方案**: 准备回滚方案
5. **监控告警**: 配置监控和告警
